// Android Edge owns only the WXT entrypoint. Platform-specific UI behavior
// lives in this app; background behavior stays in the shared runtime package.
export { default } from "@onedrop/extension-runtime/background";
