// Desktop Edge owns only the WXT entrypoint. The implementation is shared with
// the Android Edge package so both extension targets keep identical behavior.
export { default } from "@onedrop/extension-runtime/background";
