{
  "name": "onedrop",
  "version": "1.0.2",
  "private": true,
  "type": "module",
  "description": "A serverless Microsoft Edge extension for sharing through a user's OneDrive.",
  "scripts": {
    "dev": "wxt --browser edge",
    "build": "wxt build --browser edge",
    "build:android": "wxt build --browser edge --config wxt.android.config.ts --filter-entrypoint mobile --filter-entrypoint background",
    "pack:android": "npm run build:android && node scripts/pack-android.mjs",
    "build:ios-edge": "wxt build --browser edge --config wxt.ios-edge.config.ts --filter-entrypoint mobile --filter-entrypoint background",
    "pack:ios-edge": "npm run build:ios-edge && node scripts/pack-ios-edge.mjs",
    "postinstall": "patch-package",
    "build:ios": "vite build --config vite.ios.config.ts",
    "sync:ios": "npm run build:ios && cap sync ios",
    "pack:ios": "npm run sync:ios && node scripts/pack-ios-ipa.mjs",
    "open:ios": "cap open ios",
    "zip": "wxt zip --browser edge",
    "compile": "wxt prepare && tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@capacitor/core": "^8.5.0",
    "dexie": "4.4.4",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "thumbhash": "^0.1.1",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "@capacitor/ios": "^8.5.0",
    "@eslint/js": "10.0.1",
    "@testing-library/jest-dom": "7.0.0",
    "@testing-library/react": "16.3.2",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.4",
    "@vitejs/plugin-react": "^6.0.5",
    "@wxt-dev/module-react": "1.2.2",
    "eslint": "10.8.0",
    "globals": "17.8.0",
    "jsdom": "29.1.1",
    "patch-package": "^8.0.1",
    "prettier": "3.9.6",
    "typescript": "6.0.3",
    "typescript-eslint": "8.65.0",
    "vite": "^8.2.1",
    "vitest": "4.1.10",
    "wxt": "0.21.3"
  },
  "engines": {
    "node": ">=22"
  }
}
